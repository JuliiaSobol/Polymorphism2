public class Sum {
	
	public double sum(double x1, double x2) {
	    return x1 + x2;
	}
	 
	public double sum(double x1, double x2, double x3) {
	    return x1 + x2 + x3;
	}
	 
	public double sum(double x1, double x2, double x3, double x4) {
	    return x1 + x2 + x3 + x4;
	}
}
